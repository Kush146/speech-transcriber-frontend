# Frontend-Speech-Transcriber

React + Vite + Tailwind UI for recording/uploading audio and showing transcription history.

## Quickstart
```bash
cd Frontend-Speech-Transcriber
cp .env.example .env
npm i
npm run dev
```

Backend env defaults to `http://localhost:5000`.
